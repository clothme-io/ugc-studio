# API
PORT=4000
DATABASE_URL=postgresql://ugc:ugc@localhost:5433/ugc_studio
REDIS_URL=redis://localhost:6380
API_SECRET=change-me-in-production
UPLOAD_DIR=./uploads

# AI providers (at least one required for analyze/remix/AI UGC)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# AI UGC (HeyGen — required for AI UGC generation)
HEYGEN_API_KEY=

# Optional: video discovery via Apify
APIFY_API_TOKEN=

# Web (3100 avoids conflict with other ClothME apps on 3000)
WEB_PORT=3100
WEB_ORIGIN=http://localhost:3100
NEXT_PUBLIC_API_URL=http://localhost:4000

# UI mock mode (default: true). Set to false when API + DB are ready.
NEXT_PUBLIC_USE_MOCK=true
